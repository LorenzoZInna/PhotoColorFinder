import webbrowser

# Define the color code (for example, RAL 6000)
color_code = "RAL 6000"

# Create a search URL (Google search in this case)
search_url = f"https://www.google.com/search?q={color_code}+paint+industrial+color"

# Open the URL in the browser
webbrowser.open(search_url)
